export type ToolResult = string;
export class ToolUnsupportedError extends Error { constructor(slug: string) { super(`This tool (${slug}) is not implemented in this browser build.`); this.name = "ToolUnsupportedError"; } }
const json = (v: unknown) => JSON.stringify(v, null, 2);
const esc = (s: string) => s.replaceAll("&", "&amp;").replaceAll("<", "&lt;").replaceAll(">", "&gt;").replaceAll('"', "&quot;").replaceAll("'", "&#39;");
const bytes = (s: string) => new TextEncoder().encode(s);
const hex = (b: Uint8Array) => [...b].map(x => x.toString(16).padStart(2,"0")).join("");
const b64e = (s: string) => { let bin=""; for(const b of bytes(s)) bin += String.fromCharCode(b); return btoa(bin); };
const b64d = (s: string) => { const c=s.replace(/\s/g,"").replace(/-/g,"+").replace(/_/g,"/"); if(!/^[A-Za-z0-9+/]*={0,2}$/.test(c) || c.length%4===1) throw new Error("Invalid Base64."); const bin=atob(c.padEnd(Math.ceil(c.length/4)*4,"=")); return new TextDecoder().decode(Uint8Array.from(bin,x=>x.charCodeAt(0))); };
const secure = (n:number, alphabet="ABCDEFGHJKLMNPQRSTUVWXYZabcdefghijkmnopqrstuvwxyz23456789") => { if(!Number.isInteger(n)||n<1||n>4096) throw new Error("Length must be between 1 and 4096."); const out=[]; const limit=256-Math.floor(256/alphabet.length)*alphabet.length; while(out.length<n){ const r=crypto.getRandomValues(new Uint8Array(Math.max(32,n-out.length))); for(const x of r) if(x<limit && out.length<n) out.push(alphabet[x%alphabet.length]); } return out.join(""); };
const uuid=()=>crypto.randomUUID();
function parseJson(s:string){ if(!s.trim()) throw new Error("Input is empty."); try{return JSON.parse(s)}catch(e){throw new Error(e instanceof Error?e.message:"Invalid JSON.");} }
function deepSort(v:unknown):unknown { if(Array.isArray(v)) return v.map(deepSort); if(v&&typeof v==="object") return Object.fromEntries(Object.entries(v as Record<string,unknown>).sort(([a],[b])=>a.localeCompare(b)).map(([k,x])=>[k,deepSort(x)])); return v; }
function words(s:string){return s.match(/[\p{L}\p{N}]+/gu)??[];}
function slug(s:string){return s.normalize("NFKD").replace(/[\u0300-\u036f]/g,"").toLowerCase().trim().replace(/[^\p{L}\p{N}\s-]/gu,"").replace(/[\s_-]+/g,"-").replace(/^-+|-+$/g,"");}
function nums(s:string){const a=s.split(/[,\s]+/).filter(Boolean).map(Number);if(!a.length||a.some(x=>!Number.isFinite(x)))throw new Error("Enter valid numbers.");return a;}
function gcd(a:number,b:number):number{return b?gcd(b,a%b):Math.abs(a)}
function csvParse(s:string){const rows:string[][]=[];let row:string[]=[],cell="",q=false;for(let i=0;i<s.length;i++){const c=s[i];if(c==='"'&&s[i+1]==='"'&&q){cell+='"';i++;}else if(c==='"')q=!q;else if(c===','&&!q){row.push(cell);cell="";}else if((c==='\n'||c==='\r')&&!q){if(c==='\r'&&s[i+1]==='\n')i++;row.push(cell);rows.push(row);row=[];cell="";}else cell+=c;}row.push(cell);if(row.some(Boolean))rows.push(row);if(!rows.length)return[];const h=rows[0];return rows.slice(1).map(r=>Object.fromEntries(h.map((k,i)=>[k??`column${i+1}`,r[i]??""])))}
function csvOut(v:unknown){if(!Array.isArray(v)||!v.length||v.some(x=>!x||typeof x!=="object"||Array.isArray(x)))throw new Error("Expected a JSON array of objects.");const rs=v as Record<string,unknown>[];const ks=[...new Set(rs.flatMap(x=>Object.keys(x)))];const c=(x:unknown)=>`"${String(x??"").replaceAll('"','""')}"`;return [ks.map(c).join(","),...rs.map(r=>ks.map(k=>c(r[k])).join(","))].join("\n")}
function xml(v:unknown,tag="root"):string{if(v===null||typeof v!=="object")return `<${tag}>${esc(String(v??""))}</${tag}>`;if(Array.isArray(v))return v.map(x=>xml(x,"item")).join("");return `<${tag}>${Object.entries(v as Record<string,unknown>).map(([k,x])=>xml(x,k.replace(/[^\w-]/g,"_")||"item")).join("")}</${tag}>`}
function xmlJson(s:string){const d=new DOMParser().parseFromString(s,"application/xml");if(d.querySelector("parsererror"))throw new Error("Invalid XML.");const walk=(e:Element):unknown=>{const cs=[...e.children];if(!cs.length)return e.textContent??"";const o:Record<string,unknown>={};for(const c of cs){const v=walk(c);o[c.tagName]=c.tagName in o?(Array.isArray(o[c.tagName])?[...(o[c.tagName] as unknown[]),v]:[o[c.tagName],v]):v;}return o};return json({[d.documentElement.tagName]:walk(d.documentElement)})}
function jsonTs(v:unknown,name="Root"){const types:string[]=[];const seen=new Set<string>();const clean=(s:string)=>{const x=s.replace(/[^A-Za-z0-9]+(.)?/g,(_,c)=>c?c.toUpperCase():"")||"Root";return /^\d/.test(x)?`Type${x}`:x[0].toUpperCase()+x.slice(1)};const t=(x:unknown,h:string):string=>{if(x===null)return"null";if(Array.isArray(x))return x.length?`${t(x[0],h+"Item")}[]`:"unknown[]";if(typeof x==="object"){const n=clean(h);if(!seen.has(n)){seen.add(n);types.push(`export interface ${n} {\n${Object.entries(x as Record<string,unknown>).map(([k,y])=>`  ${/^[$A-Z_a-z][$\w]*$/.test(k)?k:JSON.stringify(k)}: ${t(y,k)};`).join("\n")}\n}`)}return n}return typeof x};t(v,name);return types.join("\n\n")||`export type ${clean(name)} = unknown;`}
function textStats(s:string){const w=words(s).length;return json({words:w,characters:[...s].length,charactersWithoutSpaces:[...s].filter(c=>!/\s/u.test(c)).length,sentences:s.trim()?s.split(/(?<=[.!?])\s+/u).filter(Boolean).length:0,lines:s? s.split(/\r?\n/).length:0,readingTimeMinutes:w?Math.max(1,Math.ceil(w/200)):0});}
function queryParse(s:string){const u=new URL(s.includes("?")?s:`https://eldevo.local/?${s.replace(/^\?/,"")}`);return json(Object.fromEntries(u.searchParams.entries()));}
function queryGen(s:string){const v=parseJson(s);if(!v||typeof v!=="object"||Array.isArray(v))throw new Error("Expected a JSON object.");return new URLSearchParams(Object.entries(v as Record<string,unknown>).map(([k,x])=>[k,String(x)])).toString()}
function schema(s:string,type="WebPage"){const [name="",url="",description=""]=s.split(/\r?\n/);return json({"@context":"https://schema.org","@type":type,name,url,description});}
function sitemap(s:string){const urls=s.split(/\r?\n/).map(x=>x.trim()).filter(Boolean);if(urls.some(x=>!/^https?:\/\//i.test(x)))throw new Error("Every sitemap URL must be absolute.");return `<?xml version="1.0" encoding="UTF-8"?>\n<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">\n${urls.map(u=>`  <url><loc>${esc(u)}</loc></url>`).join("\n")}\n</urlset>`}
function robots(s:string){const lines=s.split(/\r?\n/).map(x=>x.trim()).filter(Boolean);const host=lines.find(x=>x.startsWith("Host:"))?.slice(5).trim();const paths=lines.filter(x=>/^disallow:/i.test(x));return `User-agent: *\n${paths.length?paths.map(x=>x.replace(/^disallow:/i,"Disallow:")).join("\n"):"Disallow:"}${host?`\nHost: ${host}`:""}\nSitemap: ${lines.find(x=>/^sitemap:/i.test(x))?.split(/:\s*/,2)[1]??"https://example.com/sitemap.xml"}`}
function colorHex(s:string){const m=s.trim().replace(/^#/,'');if(!/^[0-9a-f]{3,8}$/i.test(m))throw new Error("Invalid HEX color.");const h=m.length===3?[...m].map(x=>x+x).join(""):m.slice(0,6);const n=parseInt(h,16);return `rgb(${n>>16}, ${(n>>8)&255}, ${n&255})`}
function percent(s:string){const a=nums(s);if(a.length!==2)throw new Error("Enter two numbers.");return `${(a[0]/100*a[1]).toPrecision(12).replace(/\.0+$/,'')} (${a[0]}% of ${a[1]})`}
function calculator(slug:string,s:string){const a=nums(s);switch(slug){case"percentage-calculator":return percent(s);case"percentage-change-calculator":if(a[0]===0)throw new Error("Original value cannot be zero.");return `${((a[1]-a[0])/Math.abs(a[0])*100).toFixed(4)}%`;case"ratio-calculator":if(a.length!==2||a[1]===0)throw new Error("Enter two non-zero numbers.");{const d=gcd(a[0],a[1]);return `${a[0]/d}:${a[1]/d}`};case"average-calculator":return String(a.reduce((x,y)=>x+y,0)/a.length);case"median-calculator":{a.sort((x,y)=>x-y);return String(a.length%2?a[(a.length-1)/2]:(a[a.length/2-1]+a[a.length/2])/2)}case"standard-deviation-calculator":{const m=a.reduce((x,y)=>x+y,0)/a.length;return String(Math.sqrt(a.reduce((x,y)=>x+(y-m)**2,0)/a.length))};case"discount-calculator":if(a.length!==2)throw new Error("Enter price and discount percent.");return String(a[0]*(1-a[1]/100));case"profit-margin-calculator":if(a[0]===0)throw new Error("Revenue cannot be zero.");return `${((a[0]-a[1])/a[0]*100).toFixed(4)}%`;case"markup-calculator":if(a[0]===0)throw new Error("Cost cannot be zero.");return `${((a[1]-a[0])/a[0]*100).toFixed(4)}%`;case"simple-interest-calculator":return String(a[0]*(1+a[1]*a[2]/100));case"compound-interest-calculator":return String(a[0]*(1+a[1]/100/a[3])**(a[3]*a[2]));default:throw new Error("Calculator inputs are not supported by this tool.")}}
export async function executeTool(slug:string,input:string):Promise<ToolResult>{if(!input.trim())throw new Error("Enter input before running the tool.");switch(slug){
case"json-formatter":case"json-beautifier":case"json-viewer":return json(parseJson(input));case"json-minifier":return JSON.stringify(parseJson(input));case"json-validator":parseJson(input);return"Valid JSON";case"json-sorter":return json(deepSort(parseJson(input)));case"json-escape":return JSON.stringify(input).slice(1,-1);case"json-unescape":return JSON.parse(`"${input}"`);case"json-diff":{const [a,b]=input.split(/\n---DIFF---\n/i);const x=parseJson(a),y=parseJson(b??"");return json({equal:JSON.stringify(x)===JSON.stringify(y),left:x,right:y})}case"json-to-csv":return csvOut(parseJson(input));case"json-to-xml":return `<?xml version="1.0" encoding="UTF-8"?>\n${xml(parseJson(input))}`;case"xml-to-json":return xmlJson(input);case"json-to-javascript":return `const data = ${json(parseJson(input))};`;case"json-to-typescript":case"json-to-typescript-interface":return jsonTs(parseJson(input));case"jsonpath-tester":case"json-path-tester":{const [j,p]=input.split(/\n---(?:JSONPATH|PATH)---\n|\n\$(?=\.)/i);let v=parseJson(j);for(const k of (p??"$").replace(/^\$\.?/,"").split(/[.\[\]]/).filter(Boolean)){if(k==="*")v=Array.isArray(v)?v:Object.values(v as Record<string,unknown>);else v=(v as Record<string,unknown>)?.[k];}return json(v)}case"json-schema-generator":{const v=parseJson(input);const properties=v&&typeof v==="object"&&!Array.isArray(v)?Object.fromEntries(Object.entries(v).map(([k,x])=>[k,{type:Array.isArray(x)?"array":x===null?"null":typeof x}])):{};return json({$schema:"https://json-schema.org/draft/2020-12/schema",type:Array.isArray(v)?"array":"object",properties})}case"json-schema-validator":{const [j,sc]=input.split(/\n---SCHEMA---\n/i);const v=parseJson(j),schema=parseJson(sc??"");const errors:string[]=[];if(schema.type&&schema.type!==(Array.isArray(v)?"array":v===null?"null":typeof v))errors.push(`$: expected ${schema.type}`);if(schema.required&&v&&typeof v==="object")for(const k of schema.required)if(!(k in(v as object)))errors.push(`$.${k}: required`);return errors.length?`Invalid\n${errors.join("\n")}`:"Valid against supported schema rules"}
case"base64-encoder":case"base64-encode-decode":return b64e(input);case"base64-decoder":return b64d(input);case"url-encoder":return encodeURIComponent(input);case"url-decoder":return decodeURIComponent(input);case"html-encoder":case"html-entity-encoder":return esc(input);case"html-decoder":{const d=document.createElement("textarea");d.innerHTML=input;return d.value}case"unicode-escape":return [...input].map(c=>[...c].map(x=>`\\u${x.codePointAt(0)!.toString(16).padStart(4,"0")}`).join("")).join("");case"unicode-decoder":return input.replace(/\\u\{?([0-9a-f]{4,6})\}?/gi,(_,h)=>String.fromCodePoint(parseInt(h,16)));case"hex-encoder":return hex(bytes(input));case"hex-decoder":{const c=input.replace(/\s/g,"");if(!/^(?:[0-9a-f]{2})*$/i.test(c))throw new Error("Hex must contain pairs of digits.");return new TextDecoder().decode(Uint8Array.from(c.match(/../g)??[],x=>parseInt(x,16)))}case"text-to-binary":return [...bytes(input)].map(x=>x.toString(2).padStart(8,"0")).join(" ");case"binary-to-text":return new TextDecoder().decode(Uint8Array.from(input.trim().split(/\s+/),x=>{const n=parseInt(x,2);if(!/^[01]{1,8}$/.test(x)||n>255)throw new Error("Invalid binary byte.");return n}));case"ascii-converter":return [...input].map(c=>`${c}: ${c.codePointAt(0)}`).join("\n");
case"sha256-generator":case"hash-generator":return `SHA-256: ${hex(new Uint8Array(await crypto.subtle.digest("SHA-256",bytes(input))))}`;case"sha512-generator":return `SHA-512: ${hex(new Uint8Array(await crypto.subtle.digest("SHA-512",bytes(input))))}`;case"sha1-generator":return `SHA-1: ${hex(new Uint8Array(await crypto.subtle.digest("SHA-1",bytes(input))))}`;case"uuid-generator":case"uuid-generator-v4":return Array.from({length:Math.min(100,Math.max(1,Number(input)||1))},uuid).join("\n");case"password-generator":return secure(32,"ABCDEFGHJKLMNPQRSTUVWXYZabcdefghijkmnopqrstuvwxyz23456789!@#$%^&*_-+=");case"random-string-generator":return secure(32);case"api-key-generator":return `eldevo_${secure(40)}`;case"secret-key-generator":return secure(64,"ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789!@#$%^&*_-+");case"hmac-generator":{const [algorithm="SHA-256",key="",message=""]=input.split(/\n---HMAC---\n/i);if(!key||!message)throw new Error("Use: algorithm\n---HMAC---\nkey\nmessage");const k=await crypto.subtle.importKey("raw",bytes(key),{name:"HMAC",hash:algorithm},false,["sign"]);return hex(new Uint8Array(await crypto.subtle.sign("HMAC",k,bytes(message))))}case"md5-generator":throw new ToolUnsupportedError(slug);
case"regex-tester":{const [p,t]=input.split(/\n---TEST---\n/i);const re=new RegExp(p,"g");return json([...t.matchAll(re)].map(m=>({match:m[0],index:m.index,groups:m.groups??{}})))}case"regex-escape":return input.replace(/[\\^$.*+?()[\]{}|]/g,"\\$&");case"email-regex-tester":return String(/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(input.trim()));case"url-regex-tester":try{new URL(input.trim());return"true"}catch{return"false"}case"regex-generator":throw new ToolUnsupportedError(slug);case"regex-explainer":{const re=new RegExp(input);return json({source:re.source,flags:re.flags,groups:(re.source.match(/\((?!\?)/g)||[]).length,note:"JavaScript RegExp parser; semantics depend on the browser engine."})}
case"html-minifier":return input.replace(/<!--[\s\S]*?-->/g,"").replace(/>\s+</g,"><").replace(/\s+/g," ").trim();case"html-formatter":return input.replace(/>\s*</g,">\n<").split("\n").map(x=>x.trim()).filter(Boolean).join("\n");case"css-minifier":return input.replace(/\/\*[\s\S]*?\*\//g,"").replace(/\s+/g," ").replace(/\s*([{}:;,>])\s*/g,"$1").trim();case"javascript-minifier":return input.replace(/\/\/.*$/gm,"").replace(/\s+/g," ").trim();case"javascript-escape":return JSON.stringify(input);case"html-validator":{const d=new DOMParser().parseFromString(input,"text/html");return json({valid:!!d.documentElement,doctype:!!input.match(/<!doctype\s+html/i),title:d.title||null,errors:[]})}case"html-to-markdown":return input.replace(/<br\s*\/?>(?:\s*)/gi,"\n").replace(/<h([1-6])>(.*?)<\/h\1>/gis,(_,n,t)=>`${"#".repeat(Number(n))} ${t}\n\n`).replace(/<strong>(.*?)<\/strong>/gis,"**$1**").replace(/<em>(.*?)<\/em>/gis,"*$1*").replace(/<p>(.*?)<\/p>/gis,"$1\n\n").replace(/<[^>]+>/g,"").trim();case"markdown-to-html":return input.split(/\r?\n\r?\n/).map(p=>`<p>${esc(p.replace(/^#{1,6}\s+/gm,"").replace(/\*\*(.*?)\*\*/g,"$1"))}</p>`).join("\n");
case"css-gradient-generator":return `background: linear-gradient(90deg, ${input||"#000 0%, #fff 100%"});`;case"css-box-shadow-generator":return `box-shadow: ${input||"0 10px 30px rgba(0,0,0,.15)"};`;case"css-border-radius-generator":return `border-radius: ${input||"12px"};`;case"css-flexbox-generator":return `display: flex;\nflex-direction: row;\njustify-content: center;\nalign-items: center;\ngap: 1rem;`;case"css-grid-generator":return `display: grid;\ngrid-template-columns: repeat(3, minmax(0, 1fr));\ngap: 1rem;`;case"javascript-formatter":return input.replace(/\s*\{\s*/g," {\n  ").replace(/\s*;\s*/g,";\n  ").replace(/\s*\}\s*/g,"\n}");
case"query-string-parser":return queryParse(input);case"query-string-generator":return queryGen(input);case"url-parser":{const u=new URL(input);return json({protocol:u.protocol,username:u.username,host:u.host,hostname:u.hostname,port:u.port||null,path:u.pathname,query:Object.fromEntries(u.searchParams.entries()),hash:u.hash})}case"domain-parser":{const u=new URL(input.includes("://")?input:`https://${input}`);const labels=u.hostname.split(".");return json({hostname:u.hostname,subdomain:labels.length>2?labels.slice(0,-2).join("."):null,domain:labels.at(-2)??null,tld:labels.at(-1)??null})}case"user-agent-parser":{const s=input;return json({browser:/Edg\//.test(s)?"Edge":/Chrome\//.test(s)?"Chrome":/Firefox\//.test(s)?"Firefox":/Safari\//.test(s)&&!/Chrome\//.test(s)?"Safari":"Unknown",os:/Windows/.test(s)?"Windows":/Android/.test(s)?"Android":/iPhone|iPad|iOS/.test(s)?"iOS":/Mac OS X/.test(s)?"macOS":/Linux/.test(s)?"Linux":"Unknown",mobile:/Mobile|Android|iPhone|iPad/i.test(s)})}case"http-status-code-checker":throw new ToolUnsupportedError(slug);
case"word-counter":case"character-counter":case"sentence-counter":case"reading-time-calculator":return textStats(input);case"email-extractor":return [...new Set(input.match(/[A-Z0-9._%+-]+@[A-Z0-9.-]+\.[A-Z]{2,}/gi)??[])].join("\n");case"url-extractor":return [...new Set(input.match(/https?:\/\/[^\s<>"]+/gi)??[])].join("\n");case"remove-duplicate-lines":return [...new Set(input.split(/\r?\n/))].join("\n");case"sort-lines":return input.split(/\r?\n/).sort((a,b)=>a.localeCompare(b)).join("\n");case"reverse-text":return [...input].reverse().join("");case"remove-extra-spaces":return input.replace(/[ \t]+/g," ").replace(/\n{3,}/g,"\n\n").trim();case"remove-empty-lines":return input.split(/\r?\n/).filter(x=>x.trim()).join("\n");case"text-diff":case"text-compare":{const [a,b]=input.split(/\n---DIFF---\n/i);return a===b?"No differences.":`Different\n--- Left ---\n${a}\n--- Right ---\n${b??""}`};case"slug-generator":return slug(input);case"lorem-ipsum-generator":return ("Lorem ipsum dolor sit amet, consectetur adipiscing elit. ").repeat(Math.min(100,Math.max(1,Number(input)||1))).trim();
case"meta-tag-generator":{const [t,d,u]=input.split(/\r?\n/);return `<title>${esc(t??"")}</title>\n<meta name="description" content="${esc(d??"")}">\n<link rel="canonical" href="${esc(u??"")}">`;}case"open-graph-generator":{const [t,d,u,img]=input.split(/\r?\n/);return `<meta property="og:title" content="${esc(t??"")}">\n<meta property="og:description" content="${esc(d??"")}">\n<meta property="og:url" content="${esc(u??"")}">\n<meta property="og:image" content="${esc(img??"")}">`;}case"twitter-card-generator":return `<meta name="twitter:card" content="summary_large_image">\n${(input.split(/\r?\n/).slice(0,3)).map((x,i)=>`<meta name="twitter:${["title","description","image"][i]}" content="${esc(x)}">`).join("\n")}`;case"schema-markup-generator":return schema(input);case"faq-schema-generator":{const pairs=input.split(/\r?\n/).map(x=>x.split("|")).filter(x=>x.length>=2);return json({"@context":"https://schema.org","@type":"FAQPage",mainEntity:pairs.map(([q,a])=>({"@type":"Question",name:q,acceptedAnswer:{"@type":"Answer",text:a}}))});}case"article-schema-generator":return schema(input,"Article");case"breadcrumb-schema-generator":return schema(input,"BreadcrumbList");case"canonical-url-generator":{const u=new URL(input);return `<link rel="canonical" href="${esc(u.href)}">`;}case"serp-snippet-preview":{const [t,d,u]=input.split(/\r?\n/);return json({title:t??"",description:d??"",url:u??"",titleLength:[...(t??"")].length,descriptionLength:[...(d??"")].length})}case"keyword-density-checker":{const [text,key]=input.split(/\n---KEYWORD---\n/i);const n=words(text).length;if(!key?.trim())throw new Error("Provide text then ---KEYWORD--- then keyword.");const k=words(key).join(" ").toLocaleLowerCase();const hits=(text.toLocaleLowerCase().match(new RegExp(k.replace(/[.*+?^${}()|[\]\\]/g,"\\$&").replace(/\s+/g,"\\s+"),"g"))??[]).length;return `${hits} occurrence(s), ${n?((hits/n)*100).toFixed(2):"0.00"}% density`;}case"heading-analyzer":{const d=new DOMParser().parseFromString(input,"text/html");return json([...d.querySelectorAll("h1,h2,h3,h4,h5,h6")].map(h=>({tag:h.tagName,text:h.textContent?.trim()??""})));}case"internal-link-analyzer":{const d=new DOMParser().parseFromString(input,"text/html");return json([...d.querySelectorAll("a[href]")].map(a=>a.getAttribute("href")).filter(Boolean).map(h=>({href:h})));}case"robots-txt-generator":return robots(input);case"sitemap-generator":return sitemap(input);
case"percentage-calculator":case"percentage-change-calculator":case"ratio-calculator":case"average-calculator":case"median-calculator":case"standard-deviation-calculator":case"compound-interest-calculator":case"simple-interest-calculator":case"discount-calculator":case"profit-margin-calculator":case"markup-calculator":return calculator(slug,input);case"age-calculator":{const d=new Date(input);if(Number.isNaN(d.getTime()))throw new Error("Enter birth date as YYYY-MM-DD.");const now=new Date();let age=now.getUTCFullYear()-d.getUTCFullYear();if(now.getUTCMonth()<d.getUTCMonth()||(now.getUTCMonth()===d.getUTCMonth()&&now.getUTCDate()<d.getUTCDate()))age--;return String(age);}case"date-difference-calculator":{const [a,b]=input.split(/\r?\n/).map(x=>new Date(x));if(a.toString()==="Invalid Date"||b.toString()==="Invalid Date")throw new Error("Enter two valid dates on separate lines.");return `${Math.abs(b.getTime()-a.getTime())/86400000} days`;}case"time-duration-calculator":{const [a,b]=input.split(/\r?\n/).map(x=>x.split(":").map(Number));if(a.length!==2||b.length!==2||[...a,...b].some(x=>!Number.isFinite(x)))throw new Error("Use HH:MM on two lines.");return `${Math.abs((b[0]*60+b[1])-(a[0]*60+a[1]))} minutes`;}case"break-even-calculator":{const [fixed,price,variable]=nums(input);if(price===variable)throw new Error("Price and variable cost cannot be equal.");return String(fixed/(price-variable));}
case"hex-color-converter":case"hex-to-rgb":return colorHex(input);case"rgb-to-hex":{const a=nums(input);if(a.length!==3||a.some(x=>x<0||x>255))throw new Error("Enter R,G,B values from 0 to 255.");return `#${a.map(x=>Math.round(x).toString(16).padStart(2,"0")).join("")}`;}case"color-picker":return colorHex(input);
case"gitignore-generator":return `# ElDevo generated .gitignore\nnode_modules/\n.next/\nout/\n.env\n.env.*\n!.env.example\ndist/\ncoverage/\n.DS_Store`;case"env-generator":return input.split(/\r?\n/).map(x=>x.trim()).filter(Boolean).map(x=>`${x}=`).join("\n");case"dockerfile-generator":return `FROM node:22-alpine\nWORKDIR /app\nCOPY package*.json ./\nRUN npm ci\nCOPY . .\nRUN npm run build\nEXPOSE 3000\nCMD [\"npm\",\"start\"]`;case"docker-compose-generator":return `services:\n  app:\n    build: .\n    ports:\n      - \"3000:3000\"\n    restart: unless-stopped`;case"cron-parser":case"cron-humanizer":{const f=input.trim().split(/\s+/);if(f.length!==5&&f.length!==6)throw new Error("Cron must contain 5 or 6 fields.");return `minute=${f.length===5?f[0]:f[1]} hour=${f.length===5?f[1]:f[2]} day=${f.length===5?f[2]:f[3]} month=${f.length===5?f[3]:f[4]} weekday=${f.length===5?f[4]:f[5]}`;}case"timestamp-generator":{const n=Number(input);if(!Number.isFinite(n))throw new Error("Enter a valid Unix timestamp.");return new Date(n*(Math.abs(n)<1e11?1000:1)).toISOString();}
case"random-number-generator":{const [min=0,max=100]=nums(input);if(max<min)throw new Error("Max must be >= min.");const x=crypto.getRandomValues(new Uint32Array(1))[0]/4294967296;return String(Math.floor(min+x*(max-min+1)));}
case"qr-code-generator":throw new ToolUnsupportedError(slug);case"image-to-base64":case"base64-to-image":case"image-resizer":case"image-cropper":case"image-compressor":case"jpg-to-png":case"png-to-jpg":case"webp-converter":case"favicon-generator":throw new ToolUnsupportedError(slug);default:throw new ToolUnsupportedError(slug);}}
